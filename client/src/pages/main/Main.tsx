import { FC } from "react";
// import FirstCarousel from "../../components/main/FirstCarousel";

const Main: FC = () => {
  // 오... 컴포넌트를 배열에 담는 게 가능하네..?

  return (
    <div>
      캐러셀을 구현할까요 아니면 그냥 랜딩페이지를 할까요 사실 역량은 부족그잡채
      but 디자인 예쁘면 좋게쑤
    </div>
  );
};

export default Main;
